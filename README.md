# This code requires the TextBlob API
In order to download this api, you can use the pip installer and run the following commands:

$ pip install -U textblob

$ python -m textblob.download_corpora lite
